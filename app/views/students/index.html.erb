<h1>Here are the students</h1>
<% @students.each do |student| %>
  <div>
    <a href="students/<%= student.id%>"><%= student.name%></a> - <%= @houses.find(student.house_id).name %>
<% end %>
  </div>
